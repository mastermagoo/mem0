# Mem0 Test Environment Configuration
# Environment: TEST
# Hardware: Mac Studio (mastermagoo.taile6d019.ts.net)
# Access: Tailscale HTTPS (mem0-test.taile6d019.ts.net)
#
# INSTRUCTIONS:
# 1. Copy this file to .env.test
# 2. Replace all <CHANGE-ME-TEST> values with real credentials
# 3. DO NOT commit .env.test to git (it's in .gitignore)
# 4. Rotate passwords every 180 days
#
# NOTE: Ports offset to avoid conflict with PRD on same hardware

# ============================================================================
# REQUIRED SECRETS (MUST CHANGE)
# ============================================================================

# PostgreSQL password (strong, can be shared with team)
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=<CHANGE-ME-TEST-PASSWORD>

# Mem0 API key (UUID recommended)
# Generate with: uuidgen
MEM0_API_KEY=<GENERATE-UUID-TEST>

# OpenAI API key (can use same as PRD for cost tracking, or separate)
OPENAI_API_KEY=<YOUR-OPENAI-API-KEY-TEST>

# Grafana admin password (can be simpler than PRD)
# Generate with: openssl rand -base64 16
GRAFANA_PASSWORD=<CHANGE-ME-TEST-GRAFANA-PASSWORD>

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

POSTGRES_DB=mem0_test
POSTGRES_USER=mem0_user_test
POSTGRES_PORT=5433

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

MEM0_PORT=8889
GRAFANA_USER=admin
GRAFANA_PORT=3001

# Environment identifier
ENVIRONMENT=test

# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================

# Storage root for all persistent data (Mac Studio NAS)
MEM0_DATA_ROOT=/Volumes/NAS/mem0-test

# ============================================================================
# TAILSCALE CONFIGURATION
# ============================================================================

# Your Tailscale network name (check with: tailscale status)
TAILNET_NAME=taile6d019.ts.net

# Tailscale serve domain prefixes (will become mem0-test.taile6d019.ts.net)
TS_SERVE_MEM0_DOMAIN=mem0-test
TS_SERVE_GRAFANA_DOMAIN=grafana-test

# Tailscale ACL tags for access control
TS_TAGS=tag:mem0-test
