# Mem0 Production Environment Configuration
# Environment: PRD
# Hardware: Mac Studio (mastermagoo.taile6d019.ts.net)
# Access: Tailscale HTTPS (mem0-prd.taile6d019.ts.net)
#
# INSTRUCTIONS:
# 1. Copy this file to .env.prd
# 2. Replace all <CHANGE-ME-PRD> values with real credentials
# 3. DO NOT commit .env.prd to git (it's in .gitignore)
# 4. Rotate passwords every 90 days (GDPR compliance)

# ============================================================================
# REQUIRED SECRETS (MUST CHANGE)
# ============================================================================

# PostgreSQL password (strong, unique to PRD)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=<CHANGE-ME-PRD-STRONG-PASSWORD>

# Mem0 API key (UUID recommended)
# Generate with: uuidgen
MEM0_API_KEY=<GENERATE-UUID-PRD>

# OpenAI API key (from https://platform.openai.com/api-keys)
OPENAI_API_KEY=<YOUR-OPENAI-API-KEY-PRD>

# Grafana admin password (strong, unique to PRD)
# Generate with: openssl rand -base64 24
GRAFANA_PASSWORD=<CHANGE-ME-PRD-GRAFANA-PASSWORD>

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

POSTGRES_DB=mem0_prd
POSTGRES_USER=mem0_user_prd
POSTGRES_PORT=5432

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

MEM0_PORT=8888
GRAFANA_USER=admin
GRAFANA_PORT=3000

# Environment identifier
ENVIRONMENT=prd

# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================

# Storage root for all persistent data (Mac Studio NAS)
MEM0_DATA_ROOT=/Volumes/NAS/mem0-prd

# ============================================================================
# TAILSCALE CONFIGURATION
# ============================================================================

# Your Tailscale network name (check with: tailscale status)
TAILNET_NAME=taile6d019.ts.net

# Tailscale serve domain prefixes (will become mem0-prd.taile6d019.ts.net)
TS_SERVE_MEM0_DOMAIN=mem0-prd
TS_SERVE_GRAFANA_DOMAIN=grafana-prd

# Tailscale ACL tags for access control
TS_TAGS=tag:mem0-prd
