version: "3.9"

# Test environment configuration
# Environment: TEST
# Hardware: Mac Studio (mastermagoo.taile6d019.ts.net)
# Access: Tailscale HTTPS (mem0-test.taile6d019.ts.net)
# Database: mem0_test
# Storage: ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: mem0_postgres_test
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mem0_test}
      POSTGRES_USER: ${POSTGRES_USER:-mem0_user_test}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:?Must set DEPLOYMENT_ENV=test in .env}
    volumes:
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/postgres:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-15432}:5432"
    networks:
      - mem0_internal_test
    labels:
      - "com.mem0-system.compose-file=docker-compose.test.yml"
      - "com.mem0-system.environment=test"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mem0_user_test}"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  mem0:
    image: ${MEM0_IMAGE:-mem0-fixed:local}
    container_name: mem0_server_test
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      neo4j:
        condition: service_healthy
    environment:
      # PostgreSQL connection (mem0 uses individual vars, not DATABASE_URL)
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-mem0_test}
      POSTGRES_USER: ${POSTGRES_USER:-mem0_user_test}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      # Neo4j connection
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USERNAME: ${NEO4J_USER:-neo4j}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?NEO4J_PASSWORD is required}
      # API keys
      OPENAI_API_KEY: ${OPENAI_API_KEY:?OPENAI_API_KEY is required}
      MEM0_API_KEY: ${MEM0_API_KEY:?MEM0_API_KEY is required}
      # Server config
      MEM0_BIND_ADDRESS: 0.0.0.0
      MEM0_PORT: ${MEM0_INTERNAL_PORT:-8888}
      HISTORY_DB_PATH: /app/data/history.db
      ENVIRONMENT: test
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:?Must set DEPLOYMENT_ENV=test in .env}
    command: ["/bin/bash", "/app/start_mem0_openai.sh"]
    volumes:
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/data:/app/data
      - ./lib/mem0_gds_patch_v2_simple.py:/app/mem0_gds_patch_v2.py:ro
      - ./scripts/start_mem0_openai.sh:/app/start_mem0_openai.sh:ro
    ports:
      - "127.0.0.1:${MEM0_PORT:-18888}:8888"
    networks:
      - mem0_internal_test
    labels:
      - "com.mem0-system.compose-file=docker-compose.test.yml"
      - "com.mem0-system.environment=test"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8888/docs"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  neo4j:
    image: neo4j:5.13.0
    container_name: mem0_neo4j_test
    restart: unless-stopped
    environment:
      NEO4J_AUTH: ${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:?NEO4J_PASSWORD is required}
      NEO4J_PLUGINS: '["graph-data-science"]'
      NEO4J_dbms_security_procedures_unrestricted: gds.*
      NEO4J_dbms_security_procedures_allowlist: gds.*
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:?Must set DEPLOYMENT_ENV=test in .env}
    ports:
      - "127.0.0.1:${NEO4J_HTTP_PORT:-17474}:7474"
      - "127.0.0.1:${NEO4J_BOLT_PORT:-17687}:7687"
    volumes:
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/neo4j:/data
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/neo4j/logs:/logs
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/neo4j/import:/var/lib/neo4j/import
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/neo4j/plugins:/plugins
    networks:
      - mem0_internal_test
    labels:
      - "com.mem0-system.compose-file=docker-compose.test.yml"
      - "com.mem0-system.environment=test"
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:7474 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  grafana:
    image: grafana/grafana:latest
    container_name: mem0_grafana_test
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:?GRAFANA_PASSWORD is required}
      GF_SERVER_ROOT_URL: https://${TS_SERVE_GRAFANA_DOMAIN:-grafana-test}.${TAILNET_NAME:-taile6d019.ts.net}
      GF_ANALYTICS_REPORTING_ENABLED: "false"
      GF_ANALYTICS_CHECK_FOR_UPDATES: "false"
      GF_USERS_ALLOW_SIGN_UP: "false"
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:?Must set DEPLOYMENT_ENV=test in .env}
    ports:
      - "127.0.0.1:${GRAFANA_PORT:-13000}:3000"
    volumes:
      - ${MEM0_DATA_ROOT:-/Users/kermit/mem0-data-test}/grafana:/var/lib/grafana
    networks:
      - mem0_internal_test
    labels:
      - "com.mem0-system.compose-file=docker-compose.test.yml"
      - "com.mem0-system.environment=test"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  telegram_bot:
    build:
      context: ./telegram_bot
    container_name: mem0_telegram_bot_test
    restart: unless-stopped
    depends_on:
      mem0:
        condition: service_healthy
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?TELEGRAM_BOT_TOKEN is required}
      # Safety: always target the internal service name on the compose network
      MEM0_URL: http://mem0:8888
      MEM0_API_KEY: ${MEM0_API_KEY:?MEM0_API_KEY is required}
      DEFAULT_NAMESPACE: ${DEFAULT_NAMESPACE:-personal}
      USER_PREFIX: ${USER_PREFIX:-mark_carey}
      MAX_RECALL_RESULTS: ${MAX_RECALL_RESULTS:-5}
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:?Must set DEPLOYMENT_ENV=test in .env}
    networks:
      - mem0_internal_test
    labels:
      - "com.mem0-system.compose-file=docker-compose.test.yml"
      - "com.mem0-system.environment=test"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mem0_internal_test:
    driver: bridge
    name: mem0_internal_test
