# Mem0 Development Environment Configuration
# Environment: DEV
# Hardware: MacBook Pro (developer laptop)
# Access: Localhost (127.0.0.1) - optional Tailscale for remote pairing
#
# INSTRUCTIONS:
# 1. Copy this file to .env.dev
# 2. Replace <YOUR-OPENAI-API-KEY> with real key
# 3. Other defaults are convenient for local development
# 4. DO NOT commit .env.dev to git (it's in .gitignore)

# ============================================================================
# REQUIRED SECRETS
# ============================================================================

# PostgreSQL password (simpler for dev, convenience over security)
POSTGRES_PASSWORD=dev_password_123

# Mem0 API key (can be static for dev)
MEM0_API_KEY=dev-api-key-12345-67890

# OpenAI API key (from https://platform.openai.com/api-keys)
# Can use same key as PRD/TEST or separate for cost tracking
OPENAI_API_KEY=<YOUR-OPENAI-API-KEY>

# Grafana admin password (simple for dev)
GRAFANA_PASSWORD=admin

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

POSTGRES_DB=mem0_dev
POSTGRES_USER=mem0_user_dev
POSTGRES_PORT=5432

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

MEM0_PORT=8888
GRAFANA_USER=admin
GRAFANA_PORT=3000

# Environment identifier
ENVIRONMENT=dev

# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================

# Storage root for all persistent data (MacBook Pro local)
# Expands to /Users/<username>/mem0-dev
MEM0_DATA_ROOT=~/mem0-dev

# ============================================================================
# TAILSCALE CONFIGURATION (OPTIONAL for remote pairing)
# ============================================================================

# Uncomment and configure if you want remote access to your DEV environment
# Useful for mob programming or remote pairing sessions

# Your Tailscale network name (check with: tailscale status)
# TAILNET_NAME=taile6d019.ts.net

# Tailscale serve domain prefixes (will become mem0-dev.taile6d019.ts.net)
# TS_SERVE_MEM0_DOMAIN=mem0-dev
# TS_SERVE_GRAFANA_DOMAIN=grafana-dev

# Tailscale ACL tags for access control
# TS_TAGS=tag:mem0-dev

# ============================================================================
# DEVELOPER NOTES
# ============================================================================

# This DEV environment is optimized for:
# - Fast iteration (no Tailscale overhead by default)
# - Offline work (no network dependencies)
# - Minimal resources (optimized for laptop battery)
# - Easy cleanup (rm -rf ~/mem0-dev to reset)
#
# To enable Tailscale access for remote pairing:
# 1. Uncomment Tailscale configuration above
# 2. Run: tailscale serve --https=mem0-dev / http://127.0.0.1:8888
# 3. Share URL: https://mem0-dev.taile6d019.ts.net
